{
  "name": "angular-signals-vs-observables",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "talks/signals-vs-observables/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/talks/signals-vs-observables",
        "postcssConfig": "talks/signals-vs-observables/postcss.config.js"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "angular-signals-vs-observables:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "angular-signals-vs-observables:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "angular-signals-vs-observables:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "angular-signals-vs-observables:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "angular-signals-vs-observables:build:development"
        },
        "production": {
          "buildTarget": "angular-signals-vs-observables:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/talks/signals-vs-observables"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "talks/signals-vs-observables/**/*.{ts,tsx,js,jsx}"
        ]
      }
    }
  },
  "tags": []
}
