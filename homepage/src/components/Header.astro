---
import { SITE_TITLE } from '../consts';
import { Astronav, MenuItems, MenuIcon } from 'astro-navbar';
import { Image } from 'astro:assets';
import avatarImage from '../images/avatar.svg';
---

<header class="lg:flex p-5 gap-5">
  <Astronav>
    <div class="flex w-full justify-between">
      <div class="flex items-center">
        <Image
          src={avatarImage}
          height="60"
          class="pr-1"
          alt="Logo of the Website, which is showing a software developer with a nice cup of coffee"
        />
        <a href="/">{SITE_TITLE}</a>
      </div>

      <div class="block lg:hidden">
        <MenuIcon class="w-4 h-4 text-white-800" />
      </div>
    </div>
    <MenuItems class="hidden lg:flex">
      <ul class="flex flex-col lg:flex-row lg:gap-5">
        <li><a href="/">Home</a></li>
        <li><a href="/about">About</a></li>
        <!--<li><a href="/blog">Blog</a></li>-->
        <!--<li>-->
        <!--  <Dropdown class="group">-->
        <!--    <button class="flex items-center">-->
        <!--      <span class="pr-1"> Content</span>-->
        <!--      <svg-->
        <!--        xmlns="http://www.w3.org/2000/svg"-->
        <!--        fill="none"-->
        <!--        viewBox="0 0 24 24"-->
        <!--        stroke-width="3"-->
        <!--        stroke="currentColor"-->
        <!--        class="w-3 h-3 mt-0.5 group-open:rotate-180"-->
        <!--      >-->
        <!--        <path-->
        <!--          stroke-linecap="round"-->
        <!--          stroke-linejoin="round"-->
        <!--          d="M19.5 8.25l-7.5 7.5-7.5-7.5"></path>-->
        <!--      </svg>-->
        <!--    </button>-->
        <!--    <DropdownItems class="relative">-->
        <!--      <div class="absolute top-0 w-full dropdown-menu">-->
        <!--        <ul class="pl-2">-->
        <!--          <li class="pt-1 pb-1"><a href="/blog">Blogs</a></li>-->
        <!--        </ul>-->
        <!--      </div>-->
        <!--    </DropdownItems>-->
        <!--  </Dropdown>-->
        <!--</li>-->
      </ul>
    </MenuItems>
  </Astronav>
</header>
<style>
  header {
    margin: 0;
    padding: 0 1em;
    background: var(--black);
    color: var(--white);
    display: flex;
    align-items: center;
    box-shadow: 0 2px 8px rgba(var(--black), 5%);
    border-bottom: 2px solid var(--dark-brown);
    text-decoration: none;
  }

  .dropdown-menu {
    background: var(--black);
  }

  h2 {
    margin: 0;
    font-size: 1em;
  }

  a,
  a.active {
    text-decoration: none;
    color: var(--white);
  }
  a:hover {
    color: var(--bright-brown);
  }
</style>
